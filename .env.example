# ====================================================================
# TEMPLATE FILE - Replace all values with your actual credentials
# ====================================================================

# MongoDB Atlas (for Tasks)
# Get from: https://cloud.mongodb.com → Connect → Connection String
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/YOUR_DATABASE?retryWrites=true&w=majority

# Neon PostgreSQL (for Users & Authentication)
# Get from: https://console.neon.tech → Your Project → Connection Details
DATABASE_URL=postgresql://YOUR_USERNAME:YOUR_PASSWORD@YOUR_HOST.neon.tech/YOUR_DATABASE?sslmode=require

# JWT Secret - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# MUST be at least 32 characters for security
JWT_SECRET=REPLACE_WITH_GENERATED_SECRET_KEY_HERE

# Email Configuration (Gmail SMTP)
# Your Gmail address
EMAIL_USER=YOUR_EMAIL@gmail.com

# Gmail App Password - NOT your regular password!
# Generate at: https://myaccount.google.com/apppasswords
EMAIL_PASS=YOUR_GMAIL_APP_PASSWORD_HERE

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS in production)
FRONTEND_URL=http://localhost:5173
